spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/test_db?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8

mybatis-plus:
  config-location: classpath:mybatis-config.xml
  mapper-locations:
    - classpath:mapping/*.xml
  global-config:
    logic-delete-value: 1
    logic-not-delete-value: 0

logging:
  level:
    root: debug